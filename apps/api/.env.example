# Prisma database connection for local development
# Visual Studio / VS Code: copy this file to `.env` in the same folder
# and adjust the credentials to match your PostgreSQL instance.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/yellbook_dev?schema=public"
